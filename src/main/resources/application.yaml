server:
  port: 8080

spring:
  application:
    name: ads-market-api
  datasource:
    url: jdbc:postgresql://localhost/ads-market
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
